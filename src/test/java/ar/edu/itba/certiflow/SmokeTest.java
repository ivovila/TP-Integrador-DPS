package ar.edu.itba.certiflow;

import static org.junit.jupiter.api.Assertions.assertTrue;

import org.junit.jupiter.api.Test;

// Verifica que el build y JUnit esten bien configurados. Borrar cuando haya tests reales.
class SmokeTest {

    @Test
    void buildRuns() {
        assertTrue(true);
    }
}
